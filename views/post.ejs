<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Article</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 0 20px;
        }

        h1 {
            font-size: 36px;
            margin-bottom: 20px;
            color: #e5e0e0;
        }

        .article {
            font-size: 18px;
            line-height: 1.8;
            color: #d3c8c8;
        }

        /* Style for the image */
        .article img {
            max-width: 100%; /* Ensure the image doesn't exceed its container width */
            display: block; /* Ensure proper spacing */
            margin-top: 20px; /* Add some space between the image and text */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= data.title %></h1>
        <!-- Check if image exists and display it -->
        <% if (data.image) { %>
            <img src="/uploads/<%= data.image %>" alt="Article Image">
        <% } %>
        <article class="article"><%= data.body %></article>
    </div>
</body>
</html>
